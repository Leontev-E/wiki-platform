# Database connection settings (e.g. your Keitaro reporting replica)
# Provide credentials for the MySQL instance that stores articles, clicks, approvals, etc.
DB_HOST=your-db-host
DB_PORT=3306
DB_USER=your-db-user
DB_PASSWORD=your-db-password
DB_NAME=your-db-name
# Optional tuning
DB_CONNECTION_LIMIT=15
DB_CHARSET=utf8mb4

# HTTP server configuration
# Set to the public URL of the React client when deploying (include protocol)
CLIENT_URL=http://localhost:5173
PORT=10000

# Redis cache (adjust the URI if you use a managed Redis or different tracker)
REDIS_URL=redis://localhost:6379
